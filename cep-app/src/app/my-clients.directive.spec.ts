import { MyClientsDirective } from './my-clients.directive';

describe('MyClientsDirective', () => {
  it('should create an instance', () => {
    const directive = new MyClientsDirective();
    expect(directive).toBeTruthy();
  });
});
